[build]
builder = "DOCKER"
dockerfile = "Dockerfile.railway"

[deploy]
startCommand = "npm start"

[deploy.healthChecks]
enabled = true
cpu = "10m"
memory = "128m"
interval = 30
timeout = 3
startPeriod = 40
retries = 3
