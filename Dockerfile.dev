FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (production-like, omit dev)
RUN npm ci --omit=dev

# Expose port
EXPOSE 3000

# Start development server
CMD ["npm", "run", "dev"]
